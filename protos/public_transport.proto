syntax = "proto3";

package PublicTransport;

service PublicTransport {
    rpc GetNextBus (NextBusRequest) returns (NextBusResponse) {}
}

message NextBusRequest {
    string bus_stop_id = 1;
}

message NextBusResponse {
    string bus_number = 1;
    string arrival_time = 2;
}
